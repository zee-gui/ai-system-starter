---
description: Import an old note, doc, or chat export into the vault, keeping only what stays useful.
---

I will give you an old document, note, or chat export. Your job: extract only what will still be
useful over time, and file it into my vault. Do not import noise. Do not invent conclusions. Flag
anything uncertain rather than asserting it.

Classify what you find into:
1. Active projects concerned (update or create the note in `1-Projects`).
2. Roles concerned (`caps`).
3. Concepts or ideas to mature (`3-Garden/ideas`, `status: seed`).
4. Resources useful later (`3-Garden/resources`).
5. Things to drop (say so, do not file them).

Rules: search the vault first so you update rather than duplicate; one atomic note per concept;
follow `4-Tools/conventions.md` and `frontmatter-spec.md`; never copy secrets. Show me your plan
before writing more than three notes, then commit.

Here is the document:
